# Pindrop release repository

The deployable pnpm workspace is [Pindrop-Production](Pindrop-Production/).
Configure that directory as Vercel's Root Directory. Release records and the
deployment analysis live in [docs](docs/).

Current release status: **NOT READY - BLOCKERS REMAIN**. See
[`docs/vercel-deployment-plan.md`](docs/vercel-deployment-plan.md) before
deploying.
