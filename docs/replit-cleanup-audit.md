# Replit Cleanup Audit — Pindrop

**Audit Date:** September 2026  
**Scope:** Complete repository inspection and cleanup of Replit-specific infrastructure, configuration, dependencies, and environment assumptions.

---

## 1. Audit Findings Summary

A comprehensive recursive scan of the repository was conducted. The following Replit-specific files, dependencies, configurations, and environment assumptions were identified.

| # | Item | File Path | What it does | Safe to remove? | Action Taken |
|---|------|-----------|--------------|-----------------|--------------|
| 1 | `.replit` | `/.replit` | Configuration file for Replit runtime, autoscale deployment target, postBuild store prune, workflows, and agent execution. | Yes | Removed file. |
| 2 | `.replitignore` | `/.replitignore` | Excludes files during Replit deployment packaging (e.g. `.local`). | Yes | Removed file. |
| 3 | `replit.md` | `/replit.md` | Replit-specific workspace instructions and Replit Agent guidance. | Yes | Removed and replaced with standalone `README.md` and `docs/local-environment.md`. |
| 4 | `.agents/` | `/.agents/` | Replit Agent directory containing an empty `skills/` folder. | Yes | Removed directory. |
| 5 | `.conversation/` | `/.conversation/` | Replit conversation state folder (empty). | Yes | Removed directory. |
| 6 | `.replit-artifact/` (api-server) | `artifacts/api-server/.replit-artifact/artifact.toml` | Replit artifact service definition for API Server (preview paths, internal IDs). | Yes | Removed directory. |
| 7 | `.replit-artifact/` (downloader) | `artifacts/pinterest-video-downloader/.replit-artifact/artifact.toml` | Replit artifact service definition for Frontend. | Yes | Removed directory. |
| 8 | `.replit-artifact/` (sandbox) | `artifacts/mockup-sandbox/.replit-artifact/artifact.toml` | Replit artifact service definition for mockup sandbox. | Yes | Removed directory. |
| 9 | `scripts/post-merge.sh` | `scripts/post-merge.sh` | Replit Git post-merge hook that triggered `db push` on Replit. | Yes | Removed file. |
| 10 | `@replit/connectors-sdk` | `package.json` | Replit platform connector SDK, entirely unused in application code. | Yes | Removed dependency from `package.json`. |
| 11 | `preinstall` bash script | `package.json` | `sh -c '...'` script requiring Unix `sh`, which fails on standard Windows local environments. | Yes | Replaced with cross-platform Node.js check. |
| 12 | `@replit/*` exclusions | `pnpm-workspace.yaml` | `minimumReleaseAgeExclude` allowlist for `@replit/*` and `stripe-replit-sync`. | Yes | Cleaned allowlist to standard packages. |
| 13 | `@replit/*` catalog entries | `pnpm-workspace.yaml` | Catalog definitions for `@replit/vite-plugin-cartographer`, `@replit/vite-plugin-dev-banner`, `@replit/vite-plugin-runtime-error-modal`. | Yes | Removed from catalog. |
| 14 | Replit Vite plugins | `artifacts/pinterest-video-downloader/package.json` | Dependencies on `@replit/vite-plugin-cartographer`, `@replit/vite-plugin-dev-banner`, `@replit/vite-plugin-runtime-error-modal`. | Yes | Removed dependencies. |
| 15 | Replit Vite plugins | `artifacts/mockup-sandbox/package.json` | Dependencies on `@replit/vite-plugin-cartographer`, `@replit/vite-plugin-dev-banner`, `@replit/vite-plugin-runtime-error-modal`. | Yes | Removed dependencies. |
| 16 | Hardcoded PORT/BASE_PATH & Replit plugins | `artifacts/pinterest-video-downloader/vite.config.ts` | Threw unhandled runtime errors if `PORT` or `BASE_PATH` environment variables were not provided by Replit; dynamically loaded Replit plugins when `REPL_ID` was set; hardcoded `@assets` alias to `attached_assets`. | Yes | Refactored into a standard Vite configuration with sensible defaults (`port: 5173`, `base: '/'`), local API proxy to `http://localhost:5000`, and removed all Replit plugins. |
| 17 | Hardcoded PORT & Replit plugins | `artifacts/mockup-sandbox/vite.config.ts` | Threw errors if `PORT` was missing; dynamically loaded Replit plugins. | Yes | Refactored into clean standard Vite config. |
| 18 | Mandatory `PORT` crash | `artifacts/api-server/src/index.ts` | Threw `new Error("PORT environment variable is required")` if `PORT` was omitted (Replit pattern). | Yes | Defaulted to standard local port `5000` (or `process.env.PORT`). |
| 19 | Restrictive CORS default | `artifacts/api-server/src/app.ts` | Defaulted to `origin: false` (blocking all browser requests) unless `FRONTEND_ORIGIN` was set. | Yes | Enabled safe local origin fallbacks (`http://localhost:5173`, etc.) in non-production environments while preserving strict `FRONTEND_ORIGIN` checks in production. |
| 20 | Health check endpoint | `artifacts/api-server/src/routes/health.ts` | Only mapped `/api/healthz`. | Partial | Mapped both `/api/healthz` and `/api/health` for standard health-check compatibility. |
| 21 | `@replit` code comments | `artifacts/pinterest-video-downloader/src/components/ui/badge.tsx` & `button.tsx` | Code comments generated by Replit's UI tool (`// @replit: no hover`, etc.). | Yes | Cleaned comments. |
| 22 | `.gitignore` Replit entries | `/.gitignore` | Lacked standard `.env`, `.env.local`, and development log ignore patterns; contained `# Replit` section. | Yes | Updated with comprehensive local development and production ignore rules. |

---

## 2. Absolute Path & URL Scan

- **`file:///` references in source code:** 0 found.
- **Hardcoded developer machine paths (`C:\Users\...`, `f:/...`):** 0 found in source files (only in temporary build logs from prior compilation).
- **`replit.dev` / `replit.app` domain references:** 0 found in source files.
- **`/home/` or `/tmp/` hardcoded paths:** 0 found.

---

## 3. Preservation Notice

- **Pinterest extraction engine:** `artifacts/api-server/src/pinterest/resolver.ts` and `security.ts` are 100% preserved. No resolver logic modified.
- **SSRF protections, DNS resolution checks, and rate limiting:** 100% preserved.
- **SEO implementation:** All 7 canonical routes, metadata, JSON-LD structured data, sitemap, and robots.txt are 100% preserved.
